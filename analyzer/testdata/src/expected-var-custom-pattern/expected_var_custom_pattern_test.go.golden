// Code generated by testifylint/internal/testgen. DO NOT EDIT.

package expectedactual

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestExpectedActualChecker(t *testing.T) {
	var result any
	var goldenValue, expected, wanted string

	// Invalid.
	{
		assert.Equal(t, goldenValue, result)                                      // want "expected-actual: need to reverse actual and expected values"
		assert.Equalf(t, goldenValue, result, "msg with args %d %s", 42, "42")    // want "expected-actual: need to reverse actual and expected values"
		assert.NotEqual(t, goldenValue, result)                                   // want "expected-actual: need to reverse actual and expected values"
		assert.NotEqualf(t, goldenValue, result, "msg with args %d %s", 42, "42") // want "expected-actual: need to reverse actual and expected values"
	}

	// Valid.
	{
		assert.Equal(t, goldenValue, result)
		assert.Equalf(t, goldenValue, result, "msg with args %d %s", 42, "42")
		assert.NotEqual(t, goldenValue, result)
		assert.NotEqualf(t, goldenValue, result, "msg with args %d %s", 42, "42")
	}

	// Not detected.
	{
		assert.Equal(t, result, expected)
		assert.Equalf(t, result, expected, "msg with args %d %s", 42, "42")
		assert.Equal(t, expected, result)
		assert.Equalf(t, expected, result, "msg with args %d %s", 42, "42")
		assert.NotEqual(t, result, wanted)
		assert.NotEqualf(t, result, wanted, "msg with args %d %s", 42, "42")
		assert.NotEqual(t, wanted, result)
		assert.NotEqualf(t, wanted, result, "msg with args %d %s", 42, "42")
	}
}
