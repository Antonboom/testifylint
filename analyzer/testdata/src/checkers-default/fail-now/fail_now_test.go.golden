// Code generated by testifylint/internal/testgen. DO NOT EDIT.

package failnow

import (
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"github.com/stretchr/testify/suite"
)

// FailNowCheckerSuite covers suite method call fixes.
type FailNowCheckerSuite struct {
	suite.Suite
}

func TestFailNowCheckerSuite(t *testing.T) {
	suite.Run(t, new(FailNowCheckerSuite))
}

func (s *FailNowCheckerSuite) TestFixSuiteMethod() {
	// Invalid (statement context – fix provided via s.T()).
	s.T().Error("failure")        // want "fail-now: use t\\.Error or t\\.Errorf instead"
	s.T().Error("extra msg")      // want "fail-now: use t\\.Error or t\\.Errorf instead"
	s.T().Errorf("fmt %s", "arg") // want "fail-now: use t\\.Error or t\\.Errorf instead"
	s.T().Errorf("fmt %s", "arg") // want "fail-now: use t\\.Error or t\\.Errorf instead"
	s.T().Fatal("failure")        // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
	s.T().Fatal("extra msg")      // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
	s.T().Fatalf("fmt %s", "arg") // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
	s.T().Fatalf("fmt %s", "arg") // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
}

func TestFailNowChecker(t *testing.T) {
	// Invalid – statement context: diagnostic + fix.
	{
		t.Error("failure")        // want "fail-now: use t\\.Error or t\\.Errorf instead"
		t.Error("extra msg")      // want "fail-now: use t\\.Error or t\\.Errorf instead"
		t.Errorf("fmt %s", "arg") // want "fail-now: use t\\.Error or t\\.Errorf instead"
		t.Errorf("fmt %s", "arg") // want "fail-now: use t\\.Error or t\\.Errorf instead"
		t.Fatal("failure")        // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
		t.Fatal("extra msg")      // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
		t.Fatalf("fmt %s", "arg") // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
		t.Fatalf("fmt %s", "arg") // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"

		t.Error("failure")        // want "fail-now: use t\\.Error or t\\.Errorf instead"
		t.Error("extra msg")      // want "fail-now: use t\\.Error or t\\.Errorf instead"
		t.Errorf("fmt %s", "arg") // want "fail-now: use t\\.Error or t\\.Errorf instead"
		t.Errorf("fmt %s", "arg") // want "fail-now: use t\\.Error or t\\.Errorf instead"
		t.Fatal("failure")        // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
		t.Fatal("extra msg")      // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
		t.Fatalf("fmt %s", "arg") // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
		t.Fatalf("fmt %s", "arg") // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
	}

	// Invalid – expression context: diagnostic only, no fix.
	{
		_ = assert.Fail(t, "failure")    // want "fail-now: use t\\.Error or t\\.Errorf instead"
		_ = assert.FailNow(t, "failure") // want "fail-now: use t\\.Fatal or t\\.Fatalf instead"
	}

	// Valid – assertion objects (non-suite method calls): ignored.
	{
		a := assert.New(t)
		a.Fail("failure")
		a.FailNow("failure")

		r := require.New(t)
		r.Fail("failure")
		r.FailNow("failure")
	}
}
